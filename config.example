# SecureFS Configuration Example
# Copy to .env and modify as needed

# Database Configuration
DB_HOST=localhost
DB_PORT=5431
DB_NAME=db
DB_USER=USER
DB_PASS=TEMP

# Server Configuration
SERVER_ADDR=127.0.0.1:8080

# Logging Level (error, warn, info, debug, trace)
RUST_LOG=info

